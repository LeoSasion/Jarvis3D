# 球体与 3D 共用枝条材质验收

final result: passed

验收日期：2026-09-10。范围：用户指定的球体与空间神经元参考中的变宽连线、圆润高光及透明质感；包含共用模式下的 2D 回归。此前记录保留在下方。

## 参照、截图与差异

- 球体：[选定参考](../docs/design/concepts/3d-neuron-sphere-concept.png)、[最终运行截图](../docs/validation/neuron-filament-sphere-preview.png)。
- 3D：[选定参考](../docs/design/concepts/3d-neuron-depth-concept.png)、[最终运行截图](../docs/validation/neuron-filament-3d-preview.png)。
- 两组均在 1672 × 941 CSS 像素、DPR 1、设置面板关闭的对应状态，将参考图和实际截图放入同一次视觉比较。另保存[2D 回归截图](../docs/validation/neuron-filament-2d-preview.png)。
- 根部现在宽于末梢，短的主核直连末梢也会收尖；跨簇轴突中间更细。线体具有暖色主体、圆润截面明暗与随曲线方向变化的窄高光。球体前后明暗和 3D 纵深分别调整。
- 修正过程：均匀亮线改为连续宽度变化；过长的白色高光改为方向相关的局部高光；亚像素细枝使用平均截面；直接连主核的叶节点补上收细处理。
- 保留 NEXUS 主题语义色、真实节点数量和原有功能界面。参考更偏金色，并有绘画式分枝和景深散焦；本轮实现偏橙红，没有加入景深模糊。带状几何的圆润感是着色模拟，不包含真实管道折射。这些差异作为后续 P3 视觉迭代项，不宣称逐像素复刻。

## 交互、数据与运行验证

- 面板提供“收尖程度”“根部增粗”“截面圆润度”“枝条通透度”。当前共用值为 85%、5.20、80%、70%；Core 宽度 0.95、发光 1.80。通透度经过 0 与 100% 调整，再恢复 70%，保存值和着色器实际值一致。
- 共用模式下切换 2D、3D、待机保持相同材质参数。3D 远近明暗当前 85%，球体为 90%；布局、相机、球体形状和标签设置仍分开保存。详细参数见[材质说明](../docs/design/concepts/neuron-filament-notes.md)。
- 仍加载 808 个笔记、7809 条来源关系。3D 默认显示 885 条、2D 为 776 条；搜索“行业报告知识地图”保留 340 条真实关联。球体的 821 条装饰关系只用于待机。
- 3D 滚轮深度 720 → 523、环绕拖动与重置正常；2D 滚轮缩放与重置正常。退出 2D 后球体使用透视相机。三种状态切换仍复用同一个 GPU 画布。
- 球体曲线仍为 19,704 个分段实例，3D 为 21,240，2D 为 18,624；每段仍为 6 个顶点，没有增加管道截面或绘制通道。新增切线和宽度缓冲及片元计算，未进行独立 FPS 基准测试。
- 动态模式检查中，球体 Y 旋转从 0.345 → 0.911 弧度，点线保持对齐，材质随球体变换。检查后恢复普通 URL 和系统减少动态效果行为。
- 已检查 1280 × 720、1920 × 1080、2560 × 1440；小视口的连线控件可滚动访问。修复了调整尺寸重建标签材质后，待机球体意外显示标签的问题；全新加载后 2560 视口的标签透明度仍为 0，探索时为 1。所有文字继续在 Bloom 后清晰绘制。
- 最终重载及操作检查没有运行异常或新增警告。修改 effect 依赖时出现过一次开发热更新提示，全新加载后未复现。临时视口覆盖已清除，预览恢复默认 1280 × 720，未发现页面横向溢出。

## 自动检查

完整 499 项测试通过；末梢收细调整后重跑相关 9 项，标签尺寸修复后重跑相关 10 项，均通过。新增测试覆盖反向关系的宽度连续性、跨簇细腰、分段接缝、4096 节点长链和球体缓冲边界。Lint、格式检查、生产构建和差异空白检查通过。构建保留现有图形包体积提示。

剩余 P0/P1/P2 阻塞项：无。覆盖本地 SDR 浏览器，未运行原生 WebView2 或 HDR 验收。

# 神经元球体与共用特效验收

final result: passed

验收日期：2026-09-10。范围：退出探索后的神经元球体，以及 2D、3D、待机共用视觉参数。此前验收记录保留在下方。

## 参照与实际画面

- 选定来源：[神经元球体概念图](../docs/design/concepts/3d-neuron-sphere-concept.png)。最终在 1672 × 941 视口，将参考和运行截图放在同一次视觉检查中对照。
- 待机已由均匀粒子球改为 14 个神经元簇构成的通透球壳：大小不一的局部亮核、球面树突、弯曲轴突和少量内部连接。轮廓由分枝形成，没有连续发光圆环或实心雾盘。
- 第一轮修正了分枝过短、等角放射过于规整及球面空隙过大的问题。当前分枝位置由实际节点数量生成，不逐像素复制概念图；细枝采样和亮核轮廓仍体现实时渲染与绘画参考的差异。
- 保留真实桌面快捷方式、工具栏及来源状态。草图中的标题、示意说明与右侧按钮不替换待机桌面；标签保持此前的无发光、无阴影、无悬停放大设计。

## 功能与数据验证

- 已启用“共用神经元样式”，并保存“启用共用神经元样式前”备份。节点/连线颜色、亮核、光晕、信号、运动与 Bloom 共用；布局、标签、相机和球体形状参数独立。
- 在 2D 关闭 NODE HALO 后，3D 面板与持久设置同步关闭；重新开启后恢复。切换视图、重载、保存/导入和恢复共用默认参数的测试通过。
- 球体大小、球面分枝展开、轴突弯曲均通过面板调整并恢复；保留密度、表层节点比例和远近明暗。新球体不使用的旧内层旋转网控件已隐藏。
- 来源仍是 808 个节点、7809 条真实关系。待机绘制 808 个来源点、821 条装饰连接；探索布局与关系查询不使用这些装饰连接。搜索“行业报告知识地图”仍可查看 340 条来源关联。
- 3D 滚轮相机 Z：720 → 523；2D 滚轮缩放：100% → 138%；各自重置正常。从 2D 退出后实际使用三维透视球壳，再进入仍恢复 2D。
- 三个状态始终只有一个 GPU 画布。待机曲线预计算后随球体变换，停用旧网线绘制和重复 CPU 曲线更新。完整动态模式检查了旋转与点线对齐；系统减少动态效果模式保持静止。临时动态测试 URL 已恢复。
- 已检查 1280 × 720、1920 × 1080、2560 × 1440 桌面视口，未发现页面横向溢出或阻碍操作的布局问题。最终恢复默认视口。
- 运行检查未发现新增浏览器警告或错误。覆盖本地 SDR 浏览器；本轮未运行原生 WebView 宿主或 HDR 显示器验收。

## 自动检查

495 项测试通过，包括球壳节点数量、连通性、曲线连续性、参数同步、隔离回退和保存恢复。Lint、格式、生产构建与差异空白检查通过。构建保留现有图形包体积提示。剩余阻塞项：无。

# 神经元 3D 模板验收

final result: passed

验收日期：2026-09-09。范围：浏览器中的三维神经元布局、独立参数及模式切换。下方保留此前的 2D 验收记录。

## 参照与实际画面

- [选定方向的空间纵深修订稿](../docs/design/concepts/3d-neuron-depth-concept.png)
- [设计、默认参数与生成提示词](../docs/design/concepts/3d-neuron-depth-notes.md)
- 入口：http://127.0.0.1:8888/ → 探索图谱 → 3D → 图谱视觉设置 → 神经元 · 3D。
- 已将草图与运行截图放在同一次视觉检查中对照；实际图谱由 808 个知识库节点生成。草图中的示意名称和簇大小不替换真实来源数据。

| 对照面 | 验收结果 |
| --- | --- |
| 构图与空间 | 多簇位于不同深度，前景主簇较大，远处簇更紧凑；枝条沿三维方向展开。旋转可见前后遮挡和视差。 |
| 节点与分枝 | 保留白热小亮核、暖橙色树突和逐级缩小的末梢；枝条曲线保留真实 XYZ 端点。 |
| 连线与交错 | 以来源关系骨架为主，加入有限的真实关系回路，跨簇曲线在不同空间平面绕行。分段曲线完整连续。 |
| 色彩与发光 | 细亮连线、局部热辉及远近明暗形成层次，保留此前校准的 Bloom；没有增加大范围圆形雾光。 |
| 文字、控件与交互 | 标签保持屏幕可读尺寸；新布局控件位于面板前部。黑橙 HUD、右侧窄栏和 2D 风格继续保留。 |

运行效果使用实时几何，分布和分枝数量由知识库决定，不追求将概念图中的示意结构逐像素复制。浏览器当前 LOW 质量下，细长曲线边缘仍受 DPR 1 采样精度影响。

## 数据与操作验证

- 本地只读 Obsidian 预览加载 808 个节点、7809 条完整来源关系；默认 3D 显示 885 条来源连线。附加交叉连线调整至 50% 时显示 958 条。全部关系查询仍保留，未添加虚构探索关系。
- 搜索“行业报告知识地图”可查看其 340 条关联。退出探索仍保留 808 个节点，球体使用独立的装饰连线。
- 空间厚度、分枝展开、绕行幅度、附加交叉连线、远近明暗均接入实际布局或着色。簇间距和分枝长度继续生效；原有节点、连线、信号和后期控件保留。
- 3D 滚轮改变相机 Z：720 → 574；重置恢复 720。2D 滚轮改变缩放：100% → 121%；重置恢复 100%。三维拖动产生真实环绕视差。
- 应用 2D 模板后，已保存的 3D 设置及特效 Profile 完全不变；切换模式和模板始终复用同一个 GPU 画布。
- 退出再进入、2D/3D 切换后标签仍可见，图集保持 2048 × 128。已修复 StrictMode 重放清理导致图集变成 1 × 1 的问题。
- 1280 × 720、1920 × 1080、2560 × 1440 三档桌面视口完成截图检查，没有页面横向溢出或阻碍操作的控件裁切。
- 最终重新加载后的浏览器检查无新增警告或错误。临时视口覆盖已恢复，预览保持打开。

## 自动检查与修正

486 项测试通过；lint、格式检查、生产构建及差异空白检查通过。构建仍有现有图形包体积提示。

除标签生命周期外，本轮修复了曲线段数变化时实例数量缓存导致的缺线，以及调整交叉连线时位置缓冲重置导致布局跳回的问题。空间算法覆盖确定性、三维分布、曲线连续性、真实关系预算、孤立节点和长链边界。

剩余阻塞项：无。验收覆盖本地浏览器；未运行原生 WPF/WebView2 宿主或 HDR 显示器验证。

# 神经元 2D 模板验收

final result: passed

验收日期：2026-09-08。范围：浏览器中的神经元 2D 关系图模板。

## 参照与实现

- [用户提供的参照图](../docs/design/references/jarvis-neuron-2d-reference.png)
- [最终预览截图](../docs/validation/neuron-2d-preview.png)
- 预览：http://localhost:8888/
- 入口：探索图谱 → 2D → 图谱视觉设置 → 神经元 · 2D。
- 已在与参照一致的 1672 × 940 视口中同时对照两张图。

| 对照面 | 验收结果 |
| --- | --- |
| 构图 | 黑色画布、多簇分布、稀疏长曲线连接、左上角标题，保留现有右侧工具栏。 |
| 节点 | 明亮的暖色中心、逐级缩小的末梢，细节呈树突分枝。 |
| 连线 | 细线随分枝弯曲，跨簇连接平滑；降低高亮后仍可辨认路径。 |
| 色彩 | 使用现有主题语义色；橙色主体、克制的光晕、可读的中性色标签。 |
| 文字与交互 | 标签保持屏幕可读尺寸，选中、查询、平移、缩放和适应窗口可用。 |

实现使用现有单一 Three.js GPU 画布，不需要装饰性位图。目录与实际关系决定聚类和分枝。参照中的“面料”等示意名称没有替换真实知识库名称；簇大小由实际内容数量决定。

## 数据与操作验证

本地只读 Obsidian 预览载入 808 个节点和 7809 条关系。模板默认绘制 776 条来自原始关系的森林边，保留全部节点及完整关系查询；不添加虚构关系。页面原有的索引摘要仍提示跳过 2 条链接。

- 模板可选中并持久保存；2D 与 3D 设置相互独立，切换后保留选择与相机缩放。
- 点击“行业报告知识地图”，可访问其 340 条关联；搜索“资料知识地图”，可查看其 44 条关联。
- 放大、拖动画布、适应窗口、退出与重新进入探索均通过。
- 1280 × 720、1920 × 1080、2560 × 1440 三档桌面视口均完成截图检查，无妨碍操作的裁切或重叠。
- 最终重载后的浏览器检查无新增警告或错误。临时视口覆盖已恢复，预览保持打开。

## 自动检查与修正

481 项测试全部通过；lint、格式检查、生产构建及差异空白检查通过。构建仍提示现有图形包体积较大。

本轮已修正上层目录聚类过密、分枝布局与真实连线不一致、标签过小、圆环过硬和跨簇连线过亮的问题。剩余阻塞项：无。

验收覆盖本地浏览器的 SDR 渲染；未运行原生 WebView 宿主或 HDR 显示器验证。
